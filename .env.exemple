# Auth
JWT_SECRET=mysecret
JWT_EXPIRATION_TIME=1800

DATABASE_HOST=db
DATABASE_PORT=5432
DATABASE_USERNAME=root
DATABASE_PASSWORD=root
DATABASE_NAME=test_db
POSTGRES_SCHEMA=public

DATABASE_URL="postgresql://${DATABASE_USERNAME}:${DATABASE_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME}?schema=${POSTGRES_SCHEMA}"

# Local Host
LOCAL_HOST=3000
BASE_URL=http://localhost:3000

# SendGrid
SENDGRID_API_KEY=chave_sendgrid
SENDGRID_SENDER_EMAIL=atila.o.rocha@gmail.com